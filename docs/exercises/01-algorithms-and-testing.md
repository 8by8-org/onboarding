# 01 - Algorithms and Testing

This exercise lives in `src/onboarding/<your-github-username>/01-algorithm.ts`.
Its test scaffold lives in
`src/__test__/onboarding/<your-github-username>/01-algorithm.test.ts`.

## Goal

Implement the algorithm in the source file and finish the test cases in the test
file.

## Relevant Scripts

- `npm run test`

## What To Do

1. Read the function contract and implement the missing behavior in
   `01-algorithm.ts`.
2. Replace the `it.todo(...)` cases in `01-algorithm.test.ts` with real tests.
3. Run `npm run test` until the generated test file passes.

## Coverage

This repo collects Vitest coverage output when you run `npm run test`.
This onboarding project does not enforce minimum coverage thresholds, but many
of our production projects do require a threshold to be met.

## Done Looks Like

- The algorithm behaves correctly.
- The test file has meaningful assertions.
- `npm run test` passes for your work.

## Next Up

Next Up: [02 - Writing TSDoc Comments](02-writing-tsdoc-comments.md)
